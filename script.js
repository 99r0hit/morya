// Instagram Feed Integration (Example using Instagram API)
document.addEventListener('DOMContentLoaded', function() {
  const instagramFeed = document.getElementById('instagram-feed');
  // Fetch Instagram posts and append to the feed
  // Example: Use Instagram API or embed code
});